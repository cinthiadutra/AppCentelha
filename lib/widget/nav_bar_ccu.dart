
import 'package:flutter/material.dart';

import '../themes/app_colors.dart';

class BtCcu extends StatelessWidget {
  const BtCcu({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: AppColors.primary,
      child: Row(
        children: [Container()],
      ),
    );
  }
}
